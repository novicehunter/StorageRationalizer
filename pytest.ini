[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --tb=short
    -q
    --strict-markers
markers =
    unit: fast unit tests with no I/O
    integration: tests that touch SQLite or filesystem
    slow: tests that are slow or require network
